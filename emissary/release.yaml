---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: emissary-crds
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: emissary-crds
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  driftDetection:
    mode: enabled

 
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: emissary-ingress
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: emissary-ingress
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  driftDetection:
    mode: enabled
  values:
    waitForApiext:
      enabled: false
